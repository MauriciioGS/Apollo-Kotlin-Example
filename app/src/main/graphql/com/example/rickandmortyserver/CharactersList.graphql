query CharactersList($page: Int){
    characters (page: $page) {
        info{
            pages
            next
        }
        results {
            id
            name
            species
            image
        }
    }
}